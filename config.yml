debug: true

rest:
  host: localhost
  port: 8080

metrics:
  host: localhost
  port: 9100
  path: /metrics

jaeger:
  service: "comments"
  host: "localhost"
  port: "6831"

database:
  host: localhost
  port: 5432
  user: postgres
  password: postgres
  name: homework-3

kafka:
  producerTopic: "comments"
  consumerTopic: "moderation_comments"
  groupId: "comments"
  brokers:
    - "localhost:9094"